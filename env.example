# Clear Trans 环境变量配置
# 复制此文件为 .env.local (本地开发) 或 .env (生产环境)

# =============================================================================
# 应用基础配置
# =============================================================================

# 应用端口
PORT=8888

# 应用信息（NEXT_PUBLIC_ 前缀的变量会暴露给客户端）
NEXT_PUBLIC_APP_NAME=Clear Trans
NEXT_PUBLIC_APP_URL=http://localhost:8888

# Google Analytics（可选，用于统计分析）
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# =============================================================================
# OpenAI 配置（服务端模式 - 提供商管理API密钥）
# =============================================================================

# OpenAI API 密钥和配置
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS=4096
OPENAI_TEMPERATURE=0.3

# 客户端模式支持（NEXT_PUBLIC_ 前缀，允许用户在前端配置）
# 注意：生产环境不建议使用，会暴露默认API密钥给客户端
# NEXT_PUBLIC_OPENAI_API_KEY=sk-your-client-openai-api-key-here
# NEXT_PUBLIC_OPENAI_BASE_URL=https://api.openai.com/v1

# =============================================================================
# Google Gemini 配置（服务端模式 - 提供商管理API密钥）
# =============================================================================

# Gemini API 密钥和配置
GEMINI_API_KEY=AIzaSy-your-gemini-api-key-here
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-2.0-flash

# =============================================================================
# 开发环境配置
# =============================================================================

# Node.js 环境
NODE_ENV=development

# 调试模式（可选）
# DEBUG=true
# VERBOSE_LOGGING=true

# =============================================================================
# 配置说明
# =============================================================================

# 1. 服务端模式 vs 客户端模式：
#    - 服务端模式：用户无需配置API密钥，使用上面的配置
#    - 客户端模式：用户需要在界面中输入自己的API密钥
#
# 2. 支持的AI提供商：
#    - OpenAI：支持翻译和TTS（语音合成）
#    - Gemini：支持翻译和TTS（语音合成）
#
# 3. TTS功能：
#    - OpenAI TTS：支持19种语音，3种模型
#    - Gemini TTS：支持30种语音，2种模型，更多风格控制
#
# 4. 温度和Token设置：
#    - OPENAI_TEMPERATURE：控制翻译的创造性（0-2，推荐0.3）
#    - OPENAI_MAX_TOKENS：单次翻译的最大Token数（推荐4096）
#
# 5. 安全注意事项：
#    - 生产环境请使用 .env 文件，不要提交到版本控制
#    - 开发环境使用 .env.local 文件
#    - NEXT_PUBLIC_ 前缀的变量会暴露给客户端，谨慎使用 